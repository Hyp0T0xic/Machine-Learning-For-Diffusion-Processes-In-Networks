"""
src — Patient Zero source detection library.

Pipeline stages:
    src.data          — network generation, epidemic simulation, cascade I/O
    src.features      — feature extraction & preprocessing for ML
    src.models        — ML model definitions (GNN, etc.)
    src.baselines     — classical graph-theory baselines
    src.evaluation    — metrics and reporting
    src.visualization — plotting utilities
"""
